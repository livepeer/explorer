import Help from "../../public/img/help.svg";
import { styled } from "@livepeer/design-system";

const StyledIcon = styled(Help, {
  color: "$neutral9",
  cursor: "pointer",
  ml: "$2",
});

export default StyledIcon;
