query treasuryProposals {
  treasuryProposals(orderBy: voteStart, orderDirection: desc) {
    id
    description
    calldatas
    targets
    values
    voteEnd
    voteStart
    proposer {
      id
    }
  }
}
